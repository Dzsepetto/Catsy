<?xml version="1.0" encoding="utf-8" ?>
    <ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Catsy.Features.Minigames.Games.Snake.SnakeView"
             BackgroundColor="Black">

    <Grid RowDefinitions="Auto,*,Auto" Padding="12">

        <Grid Row="0" ColumnDefinitions="Auto,*" Margin="0,0,0,8">
            <Button Text="Back"
                    Command="{Binding BackToMinigameSelect}"
                    Padding="10,6"/>

            <Label Grid.Column="1"
                   Text="Snake"
                   TextColor="White"
                   FontSize="18"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"/>
        </Grid>

        <Grid Row="1">

            <GraphicsView x:Name="GameView" />

            <Label x:Name="ScoreLabel"
                   Text="Score: 0"
                   TextColor="White"
                   FontSize="18"
                   Margin="10"
                   HorizontalOptions="Start"
                   VerticalOptions="Start"
                   ZIndex="10"/>

            <Label x:Name="StateLabel"
                   Text=""
                   TextColor="White"
                   FontSize="22"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   ZIndex="20"/>
        </Grid>

        <Grid Row="2" Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Up -->
            <Button Grid.Row="0" Grid.Column="1" Text="⟰" Clicked="OnUpClicked" FontSize="22" />

            <!-- Left, Restart, Right -->
            <Button Grid.Row="1" Grid.Column="0" Text="⟵" Clicked="OnLeftClicked" FontSize="22" />
            <Button Grid.Row="1" Grid.Column="1" Text="Restart" Clicked="OnRestartClicked" FontSize="18" />
            <Button Grid.Row="1" Grid.Column="2" Text="⟶" Clicked="OnRightClicked" FontSize="22" />

            <!-- Down -->
            <Button Grid.Row="2" Grid.Column="1" Text="⟱" Clicked="OnDownClicked" FontSize="22" />
        </Grid>

    </Grid>
</ContentView>